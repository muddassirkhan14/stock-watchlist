# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# Data — keep watchlist but ignore price cache / runtime prefs
data/cache.json
data/stocks.json
data/history.json
data/prefs.json

# macOS
.DS_Store

# IDE
.vscode/
.idea/
.cursor/

# Optional Docker bind-mount: corporate CA files (keep directory via certs/.gitkeep)
certs/*.pem
certs/*.crt
